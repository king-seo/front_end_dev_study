const models = require('../models')

const getTotalAge = () => {
  // const langlangs = models.langlang.findAll()
  // const total = langlangs ages total
  const total = 10

  return total
}

module.exports = {
  getTotalAge
}